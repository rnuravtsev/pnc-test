import styled from "styled-components";

const StyledFooter = styled.footer`
  grid-area: footer;
`;

const Footer = () => {
  return (
    <StyledFooter>
      Footer
    </StyledFooter>
  );
};

export default Footer;
